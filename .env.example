# Environment variables for infra-operator development
# Copy this file to .env and customize as needed

# AWS Configuration for LocalStack
AWS_ENDPOINT_URL=http://localhost:4566
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test

# Docker Registry (use ttl.sh for temporary images)
DOCKER_REGISTRY=ttl.sh

# Kubernetes Context (optional - defaults to current context)
# KUBE_CONTEXT=orbstack

# Development flags
DEBUG=true
LOG_LEVEL=debug

# Optional: Real AWS credentials for integration testing
# Uncomment and fill in if testing against real AWS
# AWS_REAL_ACCESS_KEY_ID=
# AWS_REAL_SECRET_ACCESS_KEY=
# AWS_REAL_REGION=us-east-1
