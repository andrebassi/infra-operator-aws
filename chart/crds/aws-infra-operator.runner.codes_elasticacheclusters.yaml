---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: elasticacheclusters.aws-infra-operator.runner.codes
spec:
  group: aws-infra-operator.runner.codes
  names:
    kind: ElastiCacheCluster
    listKind: ElastiCacheClusterList
    plural: elasticacheclusters
    shortNames:
    - elasticache
    singular: elasticachecluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterID
      name: Cluster
      type: string
    - jsonPath: .spec.engine
      name: Engine
      type: string
    - jsonPath: .spec.nodeType
      name: NodeType
      type: string
    - jsonPath: .status.clusterStatus
      name: Status
      type: string
    - jsonPath: .status.primaryEndpoint.address
      name: Endpoint
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ElastiCacheCluster is the Schema for the elasticacheclusters
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ElastiCacheClusterSpec defines the desired state of ElastiCacheCluster
            properties:
              atRestEncryptionEnabled:
                description: AtRestEncryptionEnabled
                type: boolean
              authTokenRef:
                description: AuthToken (for Redis with transit encryption)
                properties:
                  key:
                    description: Key within the Secret
                    type: string
                  name:
                    description: Name of the Secret
                    type: string
                  namespace:
                    description: Namespace of the Secret
                    type: string
                required:
                - key
                - name
                type: object
              automaticFailoverEnabled:
                description: AutomaticFailoverEnabled (for Redis replication groups)
                type: boolean
              clusterID:
                description: ClusterID (also used as replication group ID for Redis
                  cluster mode)
                type: string
              deletionPolicy:
                default: Delete
                description: DeletionPolicy
                enum:
                - Delete
                - Retain
                - Snapshot
                type: string
              engine:
                description: Engine (redis or memcached)
                enum:
                - redis
                - memcached
                type: string
              engineVersion:
                description: EngineVersion
                type: string
              finalSnapshotIdentifier:
                description: FinalSnapshotIdentifier (required if DeletionPolicy=Snapshot)
                type: string
              kmsKeyId:
                description: KmsKeyId for encryption at rest
                type: string
              multiAZEnabled:
                description: MultiAZEnabled
                type: boolean
              nodeType:
                description: NodeType (e.g., cache.t3.micro, cache.r6g.large)
                type: string
              notificationTopicArn:
                description: NotificationTopicArn
                type: string
              numCacheNodes:
                description: NumCacheNodes (for memcached or Redis without cluster
                  mode)
                format: int32
                minimum: 1
                type: integer
              numNodeGroups:
                description: NumNodeGroups (for Redis cluster mode)
                format: int32
                minimum: 1
                type: integer
              parameterGroupName:
                description: ParameterGroupName
                type: string
              preferredMaintenanceWindow:
                description: PreferredMaintenanceWindow
                type: string
              providerRef:
                description: ProviderRef references the AWSProvider to use
                properties:
                  name:
                    description: Name of the AWSProvider
                    type: string
                  namespace:
                    description: Namespace of the AWSProvider (if different from current
                      namespace)
                    type: string
                required:
                - name
                type: object
              replicasPerNodeGroup:
                description: ReplicasPerNodeGroup (for Redis cluster mode)
                format: int32
                maximum: 5
                minimum: 0
                type: integer
              replicationGroupDescription:
                description: ReplicationGroupDescription (for Redis replication groups)
                type: string
              securityGroupIds:
                description: SecurityGroupIds
                items:
                  type: string
                type: array
              snapshotRetentionLimit:
                description: SnapshotRetentionLimit (0-35 days)
                format: int32
                maximum: 35
                minimum: 0
                type: integer
              snapshotWindow:
                description: SnapshotWindow (e.g., "05:00-09:00")
                type: string
              subnetGroupName:
                description: SubnetGroupName
                type: string
              tags:
                additionalProperties:
                  type: string
                description: Tags
                type: object
              transitEncryptionEnabled:
                description: TransitEncryptionEnabled
                type: boolean
            required:
            - clusterID
            - engine
            - engineVersion
            - nodeType
            - providerRef
            type: object
          status:
            description: ElastiCacheClusterStatus defines the observed state of ElastiCacheCluster
            properties:
              cacheClusterARN:
                description: CacheClusterARN
                type: string
              cacheNodeType:
                description: CacheNodeType
                type: string
              clusterCreateTime:
                description: ClusterCreateTime
                format: date-time
                type: string
              clusterStatus:
                description: ClusterStatus (creating, available, modifying, deleting,
                  etc)
                type: string
              conditions:
                description: Conditions
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              configurationEndpoint:
                description: ConfigurationEndpoint (for cluster mode)
                properties:
                  address:
                    description: Address
                    type: string
                  port:
                    description: Port
                    format: int32
                    type: integer
                required:
                - address
                - port
                type: object
              engineVersion:
                description: EngineVersion
                type: string
              lastSyncTime:
                description: LastSyncTime
                format: date-time
                type: string
              memberClusters:
                description: MemberClusters
                items:
                  type: string
                type: array
              nodeEndpoints:
                description: NodeEndpoints (individual node endpoints)
                items:
                  description: CacheEndpoint defines a cache endpoint
                  properties:
                    address:
                      description: Address
                      type: string
                    port:
                      description: Port
                      format: int32
                      type: integer
                  required:
                  - address
                  - port
                  type: object
                type: array
              primaryEndpoint:
                description: PrimaryEndpoint
                properties:
                  address:
                    description: Address
                    type: string
                  port:
                    description: Port
                    format: int32
                    type: integer
                required:
                - address
                - port
                type: object
              readerEndpoint:
                description: ReaderEndpoint
                properties:
                  address:
                    description: Address
                    type: string
                  port:
                    description: Port
                    format: int32
                    type: integer
                required:
                - address
                - port
                type: object
              ready:
                description: Ready
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
