---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: vpcs.aws-infra-operator.runner.codes
spec:
  group: aws-infra-operator.runner.codes
  names:
    kind: VPC
    listKind: VPCList
    plural: vpcs
    shortNames:
    - vpc
    singular: vpc
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.vpcID
      name: VPC-ID
      type: string
    - jsonPath: .status.cidrBlock
      name: CIDR
      type: string
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VPC is the Schema for the vpcs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VPCSpec defines the desired state of VPC
            properties:
              cidrBlock:
                description: CidrBlock is the IPv4 CIDR block for the VPC
                pattern: ^([0-9]{1,3}\.){3}[0-9]{1,3}/[0-9]{1,2}$
                type: string
              deletionPolicy:
                default: Delete
                description: DeletionPolicy
                enum:
                - Delete
                - Retain
                type: string
              enableDnsHostnames:
                default: true
                description: EnableDnsHostnames indicates whether instances launched
                  in the VPC get DNS hostnames
                type: boolean
              enableDnsSupport:
                default: true
                description: EnableDnsSupport indicates whether DNS resolution is
                  supported
                type: boolean
              instanceTenancy:
                default: default
                description: InstanceTenancy is the tenancy option for instances launched
                  into the VPC
                enum:
                - default
                - dedicated
                - host
                type: string
              providerRef:
                description: ProviderRef references the AWSProvider to use
                properties:
                  name:
                    description: Name of the AWSProvider
                    type: string
                  namespace:
                    description: Namespace of the AWSProvider (if different from current
                      namespace)
                    type: string
                required:
                - name
                type: object
              tags:
                additionalProperties:
                  type: string
                description: Tags to apply to the VPC
                type: object
            required:
            - cidrBlock
            - providerRef
            type: object
          status:
            description: VPCStatus defines the observed state of VPC
            properties:
              cidrBlock:
                description: CidrBlock is the primary IPv4 CIDR block
                type: string
              isDefault:
                description: IsDefault indicates if this is the default VPC
                type: boolean
              lastSyncTime:
                description: LastSyncTime
                format: date-time
                type: string
              ready:
                description: Ready indicates if the VPC is ready
                type: boolean
              state:
                description: State is the current state of the VPC
                type: string
              vpcID:
                description: VpcID is the ID of the VPC
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
