---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: certificates.aws-infra-operator.runner.codes
spec:
  group: aws-infra-operator.runner.codes
  names:
    kind: Certificate
    listKind: CertificateList
    plural: certificates
    shortNames:
    - cert
    - certs
    singular: certificate
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.domainName
      name: Domain
      type: string
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CertificateSpec defines the desired state of Certificate
            properties:
              deletionPolicy:
                default: Delete
                description: DeletionPolicy
                enum:
                - Delete
                - Retain
                type: string
              domainName:
                description: DomainName is the fully qualified domain name
                type: string
              providerRef:
                description: ProviderReference references an AWSProvider resource
                properties:
                  name:
                    description: Name of the AWSProvider
                    type: string
                  namespace:
                    description: Namespace of the AWSProvider (if different from current
                      namespace)
                    type: string
                required:
                - name
                type: object
              subjectAlternativeNames:
                description: SubjectAlternativeNames are additional FQDNs
                items:
                  type: string
                type: array
              tags:
                additionalProperties:
                  type: string
                description: Tags to apply
                type: object
              validationMethod:
                default: DNS
                description: ValidationMethod for domain ownership
                enum:
                - DNS
                - EMAIL
                type: string
            required:
            - domainName
            - providerRef
            type: object
          status:
            description: CertificateStatus defines the observed state
            properties:
              certificateARN:
                type: string
              lastSyncTime:
                format: date-time
                type: string
              ready:
                type: boolean
              status:
                type: string
              validationRecords:
                items:
                  properties:
                    domainName:
                      type: string
                    resourceRecordName:
                      type: string
                    resourceRecordType:
                      type: string
                    resourceRecordValue:
                      type: string
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
