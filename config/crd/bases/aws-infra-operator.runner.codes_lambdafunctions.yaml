---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: lambdafunctions.aws-infra-operator.runner.codes
spec:
  group: aws-infra-operator.runner.codes
  names:
    kind: LambdaFunction
    listKind: LambdaFunctionList
    plural: lambdafunctions
    shortNames:
    - lambda
    singular: lambdafunction
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.functionName
      name: Function
      type: string
    - jsonPath: .spec.runtime
      name: Runtime
      type: string
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LambdaFunction is the Schema for the lambdafunctions API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LambdaFunctionSpec defines the desired state of LambdaFunction
            properties:
              code:
                description: Code defines the function code source
                properties:
                  imageUri:
                    description: ImageUri for container image (e.g., ECR)
                    type: string
                  s3Bucket:
                    description: S3Bucket for code stored in S3
                    type: string
                  s3Key:
                    description: S3Key is the object key in S3
                    type: string
                  s3ObjectVersion:
                    description: S3ObjectVersion for versioned S3 objects
                    type: string
                  zipFile:
                    description: ZipFile for inline code (base64 encoded zip)
                    type: string
                type: object
              deletionPolicy:
                description: |-
                  DeletionPolicy determines what happens when the CR is deleted
                  Valid values: Delete (default), Retain
                type: string
              description:
                description: Description is an optional description of the function
                type: string
              environment:
                description: Environment variables for the function
                properties:
                  variables:
                    additionalProperties:
                      type: string
                    type: object
                type: object
              functionName:
                description: FunctionName is the name of the Lambda function
                type: string
              handler:
                description: Handler is the function entry point (e.g., index.handler,
                  main)
                type: string
              layers:
                description: Layers are ARNs of Lambda layers
                items:
                  type: string
                type: array
              memorySize:
                description: MemorySize in MB (128-10240, default 128)
                format: int32
                type: integer
              providerRef:
                description: ProviderRef references the AWSProvider for authentication
                properties:
                  name:
                    description: Name of the AWSProvider
                    type: string
                  namespace:
                    description: Namespace of the AWSProvider (if different from current
                      namespace)
                    type: string
                required:
                - name
                type: object
              role:
                description: Role is the ARN of the IAM role for Lambda execution
                type: string
              runtime:
                description: Runtime is the Lambda runtime (e.g., python3.12, nodejs20.x,
                  go1.x)
                type: string
              tags:
                additionalProperties:
                  type: string
                description: Tags for the Lambda function
                type: object
              timeout:
                description: Timeout in seconds (1-900, default 3)
                format: int32
                type: integer
              vpcConfig:
                description: VpcConfig for running Lambda in VPC
                properties:
                  securityGroupIds:
                    items:
                      type: string
                    type: array
                  subnetIds:
                    items:
                      type: string
                    type: array
                required:
                - securityGroupIds
                - subnetIds
                type: object
            required:
            - code
            - functionName
            - handler
            - providerRef
            - role
            - runtime
            type: object
          status:
            description: LambdaFunctionStatus defines the observed state of LambdaFunction
            properties:
              codeSize:
                description: CodeSize in bytes
                format: int64
                type: integer
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              functionArn:
                description: FunctionArn is the ARN of the Lambda function
                type: string
              lastModified:
                description: LastModified timestamp
                type: string
              lastSyncTime:
                description: LastSyncTime is when the function was last synced
                format: date-time
                type: string
              ready:
                description: Ready indicates if the function is ready
                type: boolean
              state:
                description: State is the current state (Pending, Active, Inactive,
                  Failed)
                type: string
              stateReason:
                description: StateReason provides details about the state
                type: string
              version:
                description: Version is the published version
                type: string
            required:
            - ready
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
