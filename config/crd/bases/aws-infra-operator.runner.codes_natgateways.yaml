---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: natgateways.aws-infra-operator.runner.codes
spec:
  group: aws-infra-operator.runner.codes
  names:
    kind: NATGateway
    listKind: NATGatewayList
    plural: natgateways
    shortNames:
    - natgw
    singular: natgateway
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.natGatewayID
      name: NAT-ID
      type: string
    - jsonPath: .status.subnetID
      name: Subnet-ID
      type: string
    - jsonPath: .status.publicIP
      name: PublicIP
      type: string
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NATGateway is the Schema for the natgateways API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NATGatewaySpec defines the desired state of NATGateway
            properties:
              allocationID:
                description: AllocationID is the Elastic IP allocation ID (optional,
                  will be created if not provided)
                type: string
              connectivityType:
                default: public
                description: ConnectivityType specifies if the NAT gateway is public
                  or private
                enum:
                - public
                - private
                type: string
              deletionPolicy:
                default: Delete
                description: DeletionPolicy
                enum:
                - Delete
                - Retain
                type: string
              providerRef:
                description: ProviderRef references the AWSProvider to use
                properties:
                  name:
                    description: Name of the AWSProvider
                    type: string
                  namespace:
                    description: Namespace of the AWSProvider (if different from current
                      namespace)
                    type: string
                required:
                - name
                type: object
              subnetID:
                description: SubnetID is the ID of the subnet for the NAT gateway
                type: string
              tags:
                additionalProperties:
                  type: string
                description: Tags to apply to the NAT gateway
                type: object
            required:
            - providerRef
            - subnetID
            type: object
          status:
            description: NATGatewayStatus defines the observed state of NATGateway
            properties:
              lastSyncTime:
                description: LastSyncTime
                format: date-time
                type: string
              natGatewayID:
                description: NatGatewayID is the ID of the NAT gateway
                type: string
              privateIP:
                description: PrivateIP is the private IP address
                type: string
              publicIP:
                description: PublicIP is the Elastic IP address
                type: string
              ready:
                description: Ready indicates if the NAT gateway is ready
                type: boolean
              state:
                description: State is the current state of the NAT gateway
                type: string
              subnetID:
                description: SubnetID is the ID of the subnet
                type: string
              vpcID:
                description: VpcID is the ID of the VPC
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
