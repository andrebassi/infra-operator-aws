---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: elasticips.aws-infra-operator.runner.codes
spec:
  group: aws-infra-operator.runner.codes
  names:
    kind: ElasticIP
    listKind: ElasticIPList
    plural: elasticips
    shortNames:
    - eip
    - eips
    singular: elasticip
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.publicIP
      name: Public IP
      type: string
    - jsonPath: .status.allocationID
      name: Allocation ID
      type: string
    - jsonPath: .status.instanceID
      name: Associated
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ElasticIP is the Schema for the elasticips API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ElasticIPSpec defines the desired state of ElasticIP
            properties:
              customerOwnedIpv4Pool:
                description: CustomerOwnedIpv4Pool is the ID of a customer-owned address
                  pool
                type: string
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy determines what happens to the EIP when the CR is deleted
                  Valid values: Delete, Retain
                enum:
                - Delete
                - Retain
                type: string
              domain:
                default: vpc
                description: |-
                  Domain for the Elastic IP address
                  Valid values: vpc (default), standard
                enum:
                - vpc
                - standard
                type: string
              networkBorderGroup:
                description: NetworkBorderGroup is the unique set of Availability
                  Zones from which AWS advertises IP addresses
                type: string
              providerRef:
                description: ProviderRef references the AWSProvider for authentication
                properties:
                  name:
                    description: Name of the AWSProvider
                    type: string
                  namespace:
                    description: Namespace of the AWSProvider (if different from current
                      namespace)
                    type: string
                required:
                - name
                type: object
              publicIpv4Pool:
                description: PublicIpv4Pool is the ID of an address pool to allocate
                  the address from
                type: string
              tags:
                additionalProperties:
                  type: string
                description: Tags to apply to the Elastic IP
                type: object
            required:
            - providerRef
            type: object
          status:
            description: ElasticIPStatus defines the observed state of ElasticIP
            properties:
              allocationID:
                description: AllocationID is the ID for the allocation
                type: string
              associationID:
                description: AssociationID if the EIP is associated with an instance
                  or network interface
                type: string
              domain:
                description: Domain (vpc or standard)
                type: string
              instanceID:
                description: InstanceID of the instance the address is associated
                  with
                type: string
              lastSyncTime:
                description: LastSyncTime is the last time the EIP was synced
                format: date-time
                type: string
              networkInterfaceID:
                description: NetworkInterfaceID of the network interface the address
                  is associated with
                type: string
              privateIPAddress:
                description: PrivateIPAddress associated with the Elastic IP address
                type: string
              publicIP:
                description: PublicIP is the Elastic IP address
                type: string
              ready:
                description: Ready indicates if the Elastic IP is allocated
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
