---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: iamroles.aws-infra-operator.runner.codes
spec:
  group: aws-infra-operator.runner.codes
  names:
    kind: IAMRole
    listKind: IAMRoleList
    plural: iamroles
    singular: iamrole
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.roleName
      name: Role
      type: string
    - jsonPath: .status.roleArn
      name: ARN
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: IAMRole is the Schema for the iamroles API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: IAMRoleSpec defines the desired state of IAMRole
            properties:
              assumeRolePolicyDocument:
                description: AssumeRolePolicyDocument is the trust policy that grants
                  permission to assume this role
                type: string
              deletionPolicy:
                default: Delete
                description: DeletionPolicy determines what happens to the AWS resource
                  when the CR is deleted
                enum:
                - Delete
                - Retain
                - Orphan
                type: string
              description:
                description: Description of the IAM role
                type: string
              inlinePolicy:
                description: InlinePolicy defines an inline policy to embed in the
                  role
                properties:
                  policyDocument:
                    description: PolicyDocument is the JSON policy document
                    type: string
                  policyName:
                    description: PolicyName is the name of the inline policy
                    type: string
                required:
                - policyDocument
                - policyName
                type: object
              managedPolicyArns:
                description: ManagedPolicyArns is a list of managed policy ARNs to
                  attach to the role
                items:
                  type: string
                type: array
              maxSessionDuration:
                description: |-
                  MaxSessionDuration is the maximum session duration (in seconds) for the role
                  Valid values: 3600 (1 hour) to 43200 (12 hours)
                format: int32
                maximum: 43200
                minimum: 3600
                type: integer
              path:
                description: Path is the path to the role
                type: string
              permissionsBoundary:
                description: PermissionsBoundary is the ARN of the policy that sets
                  the permissions boundary for the role
                type: string
              providerRef:
                description: ProviderRef references the AWSProvider to use for this
                  resource
                properties:
                  name:
                    description: Name of the AWSProvider
                    type: string
                  namespace:
                    description: Namespace of the AWSProvider (if different from current
                      namespace)
                    type: string
                required:
                - name
                type: object
              roleName:
                description: RoleName is the name of the IAM role
                type: string
              tags:
                additionalProperties:
                  type: string
                description: Tags to apply to the IAM role
                type: object
            required:
            - assumeRolePolicyDocument
            - providerRef
            - roleName
            type: object
          status:
            description: IAMRoleStatus defines the observed state of IAMRole
            properties:
              createdAt:
                description: CreatedAt is when the role was created
                format: date-time
                type: string
              lastSyncTime:
                description: LastSyncTime is when the role was last synced
                format: date-time
                type: string
              message:
                description: Message provides additional information about the role
                  status
                type: string
              ready:
                description: Ready indicates whether the IAM role is ready
                type: boolean
              roleArn:
                description: RoleArn is the ARN of the IAM role
                type: string
              roleId:
                description: RoleId is the stable and unique string identifying the
                  role
                type: string
            required:
            - ready
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
