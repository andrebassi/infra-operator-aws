---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: apigateways.aws-infra-operator.runner.codes
spec:
  group: aws-infra-operator.runner.codes
  names:
    kind: APIGateway
    listKind: APIGatewayList
    plural: apigateways
    singular: apigateway
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.apiId
      name: API ID
      type: string
    - jsonPath: .status.protocolType
      name: Protocol
      type: string
    - jsonPath: .status.apiEndpoint
      name: Endpoint
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: APIGateway is the Schema for the apigateways API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: APIGatewaySpec defines the desired state of APIGateway
            properties:
              corsConfiguration:
                description: CorsConfiguration for HTTP APIs
                properties:
                  allowCredentials:
                    description: AllowCredentials indicates whether credentials are
                      allowed
                    type: boolean
                  allowHeaders:
                    description: AllowHeaders is a list of allowed HTTP headers
                    items:
                      type: string
                    type: array
                  allowMethods:
                    description: AllowMethods is a list of allowed HTTP methods
                    items:
                      type: string
                    type: array
                  allowOrigins:
                    description: AllowOrigins is a list of allowed origins
                    items:
                      type: string
                    type: array
                  exposeHeaders:
                    description: ExposeHeaders is a list of exposed HTTP headers
                    items:
                      type: string
                    type: array
                  maxAge:
                    description: MaxAge is the max age for CORS in seconds
                    format: int32
                    type: integer
                type: object
              deletionPolicy:
                default: Delete
                description: DeletionPolicy determines how to handle the API on CR
                  deletion
                enum:
                - Delete
                - Retain
                - Orphan
                type: string
              description:
                description: Description of the API
                type: string
              disableExecuteApiEndpoint:
                description: DisableExecuteApiEndpoint disables the default execute-api
                  endpoint
                type: boolean
              endpointType:
                default: REGIONAL
                description: EndpointType specifies the endpoint type
                enum:
                - REGIONAL
                - EDGE
                - PRIVATE
                type: string
              name:
                description: Name of the API Gateway
                type: string
              protocolType:
                default: REST
                description: ProtocolType specifies the API protocol
                enum:
                - REST
                - HTTP
                - WEBSOCKET
                type: string
              providerRef:
                description: ProviderRef references the AWSProvider for credentials
                properties:
                  name:
                    description: Name of the AWSProvider
                    type: string
                  namespace:
                    description: Namespace of the AWSProvider (if different from current
                      namespace)
                    type: string
                required:
                - name
                type: object
              tags:
                additionalProperties:
                  type: string
                description: Tags to apply to the API Gateway
                type: object
            required:
            - name
            - providerRef
            type: object
          status:
            description: APIGatewayStatus defines the observed state of APIGateway
            properties:
              apiEndpoint:
                description: APIEndpoint is the invoke URL
                type: string
              apiId:
                description: APIID is the AWS API Gateway ID
                type: string
              lastSyncTime:
                description: LastSyncTime is the last time the resource was synced
                format: date-time
                type: string
              protocolType:
                description: ProtocolType is the protocol type
                type: string
              ready:
                description: Ready indicates if the API Gateway is ready
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
