---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: ec2instances.aws-infra-operator.runner.codes
spec:
  group: aws-infra-operator.runner.codes
  names:
    kind: EC2Instance
    listKind: EC2InstanceList
    plural: ec2instances
    shortNames:
    - ec2
    singular: ec2instance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.instanceName
      name: Instance
      type: string
    - jsonPath: .spec.instanceType
      name: Type
      type: string
    - jsonPath: .status.instanceState
      name: State
      type: string
    - jsonPath: .status.instanceID
      name: InstanceID
      type: string
    - jsonPath: .status.privateIP
      name: PrivateIP
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EC2Instance is the Schema for the ec2instances API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EC2InstanceSpec defines the desired state of EC2Instance
            properties:
              blockDeviceMappings:
                description: BlockDeviceMappings for EBS volumes
                items:
                  description: BlockDeviceMapping defines an EBS volume mapping
                  properties:
                    deviceName:
                      description: DeviceName (e.g., /dev/sda1, /dev/xvdf)
                      type: string
                    ebs:
                      description: EBS configuration
                      properties:
                        deleteOnTermination:
                          default: true
                          description: DeleteOnTermination
                          type: boolean
                        encrypted:
                          description: Encrypted
                          type: boolean
                        iops:
                          description: IOPS for io1/io2
                          format: int32
                          type: integer
                        kmsKeyID:
                          description: KMSKeyID for encryption
                          type: string
                        volumeSize:
                          description: VolumeSize in GB
                          format: int32
                          type: integer
                        volumeType:
                          default: gp3
                          description: VolumeType (gp2, gp3, io1, io2, st1, sc1)
                          enum:
                          - gp2
                          - gp3
                          - io1
                          - io2
                          - st1
                          - sc1
                          - standard
                          type: string
                      required:
                      - volumeSize
                      type: object
                  required:
                  - deviceName
                  type: object
                type: array
              deletionPolicy:
                default: Delete
                description: DeletionPolicy
                enum:
                - Delete
                - Retain
                - Stop
                type: string
              disableApiTermination:
                description: DisableAPITermination
                type: boolean
              ebsOptimized:
                description: EBSOptimized
                type: boolean
              enableConsoleOutput:
                description: |-
                  EnableConsoleOutput habilita a coleta de logs do console da EC2
                  Os logs serão armazenados no status.consoleOutput
                type: boolean
              iamInstanceProfile:
                description: IAMInstanceProfile
                type: string
              imageID:
                description: AMI ID
                type: string
              instanceName:
                description: InstanceName is a friendly name for the instance
                type: string
              instanceType:
                description: InstanceType (t3.micro, t3.small, m5.large, etc)
                type: string
              keyName:
                description: KeyName for SSH access
                type: string
              monitoring:
                description: Monitoring enables detailed CloudWatch monitoring
                type: boolean
              providerRef:
                description: ProviderRef references the AWSProvider to use
                properties:
                  name:
                    description: Name of the AWSProvider
                    type: string
                  namespace:
                    description: Namespace of the AWSProvider (if different from current
                      namespace)
                    type: string
                required:
                - name
                type: object
              securityGroupIDs:
                description: SecurityGroupIDs
                items:
                  type: string
                type: array
              subnetID:
                description: SubnetID
                type: string
              tags:
                additionalProperties:
                  type: string
                description: Tags
                type: object
              userData:
                description: UserData script (base64 encoded will be handled by controller)
                type: string
            required:
            - imageID
            - instanceName
            - instanceType
            - providerRef
            type: object
          status:
            description: EC2InstanceStatus defines the observed state of EC2Instance
            properties:
              availabilityZone:
                description: AvailabilityZone
                type: string
              conditions:
                description: Conditions
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              consoleOutput:
                description: |-
                  ConsoleOutput contém as últimas linhas do console output da EC2
                  Preenchido apenas quando spec.enableConsoleOutput=true
                type: string
              consoleOutputTimestamp:
                description: ConsoleOutputTimestamp é o timestamp do último console
                  output
                format: date-time
                type: string
              instanceID:
                description: InstanceID
                type: string
              instanceState:
                description: InstanceState (pending, running, stopping, stopped, terminated)
                type: string
              lastSyncTime:
                description: LastSyncTime
                format: date-time
                type: string
              launchTime:
                description: LaunchTime
                format: date-time
                type: string
              privateDNS:
                description: PrivateDNS
                type: string
              privateIP:
                description: PrivateIP
                type: string
              publicDNS:
                description: PublicDNS
                type: string
              publicIP:
                description: PublicIP
                type: string
              ready:
                description: Ready
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
