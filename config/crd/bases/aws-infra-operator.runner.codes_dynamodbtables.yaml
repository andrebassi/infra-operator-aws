---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: dynamodbtables.aws-infra-operator.runner.codes
spec:
  group: aws-infra-operator.runner.codes
  names:
    kind: DynamoDBTable
    listKind: DynamoDBTableList
    plural: dynamodbtables
    shortNames:
    - ddb
    - ddbtable
    singular: dynamodbtable
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.tableName
      name: Table
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .status.tableStatus
      name: Status
      type: string
    - jsonPath: .status.itemCount
      name: Items
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DynamoDBTable is the Schema for the dynamodbtables API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DynamoDBTableSpec defines the desired state of DynamoDBTable
            properties:
              attributes:
                description: Attributes defines all attributes used in keys
                items:
                  description: AttributeDefinition defines a DynamoDB attribute
                  properties:
                    name:
                      description: Name of the attribute
                      type: string
                    type:
                      description: Type of the attribute (S=String, N=Number, B=Binary)
                      enum:
                      - S
                      - "N"
                      - B
                      type: string
                  required:
                  - name
                  - type
                  type: object
                type: array
              billingMode:
                description: BillingMode controls how you are charged for read and
                  write throughput
                enum:
                - PROVISIONED
                - PAY_PER_REQUEST
                type: string
              deletionPolicy:
                default: Delete
                description: DeletionPolicy determines behavior on CR deletion
                enum:
                - Delete
                - Retain
                type: string
              globalSecondaryIndexes:
                description: GlobalSecondaryIndexes defines GSIs
                items:
                  description: GlobalSecondaryIndex defines a GSI
                  properties:
                    hashKey:
                      description: HashKey for the index
                      type: string
                    indexName:
                      description: IndexName is the name of the index
                      type: string
                    nonKeyAttributes:
                      description: NonKeyAttributes to include in projection (for
                        INCLUDE type)
                      items:
                        type: string
                      type: array
                    projectionType:
                      description: ProjectionType determines which attributes are
                        projected
                      enum:
                      - ALL
                      - KEYS_ONLY
                      - INCLUDE
                      type: string
                    rangeKey:
                      description: RangeKey for the index (optional)
                      type: string
                  required:
                  - hashKey
                  - indexName
                  - projectionType
                  type: object
                type: array
              hashKey:
                description: HashKey is the partition key attribute
                properties:
                  name:
                    description: Name of the attribute
                    type: string
                  type:
                    description: Type of the attribute (S=String, N=Number, B=Binary)
                    enum:
                    - S
                    - "N"
                    - B
                    type: string
                required:
                - name
                - type
                type: object
              pointInTimeRecovery:
                description: PointInTimeRecovery enables point-in-time recovery
                type: boolean
              providerRef:
                description: ProviderRef references the AWSProvider
                properties:
                  name:
                    description: Name of the AWSProvider
                    type: string
                  namespace:
                    description: Namespace of the AWSProvider (if different from current
                      namespace)
                    type: string
                required:
                - name
                type: object
              rangeKey:
                description: RangeKey is the sort key attribute (optional)
                properties:
                  name:
                    description: Name of the attribute
                    type: string
                  type:
                    description: Type of the attribute (S=String, N=Number, B=Binary)
                    enum:
                    - S
                    - "N"
                    - B
                    type: string
                required:
                - name
                - type
                type: object
              streamEnabled:
                description: StreamEnabled enables DynamoDB Streams
                type: boolean
              streamViewType:
                description: StreamViewType determines what information is written
                  to the stream
                enum:
                - NEW_IMAGE
                - OLD_IMAGE
                - NEW_AND_OLD_IMAGES
                - KEYS_ONLY
                type: string
              tableName:
                description: TableName is the name of the DynamoDB table
                type: string
              tags:
                additionalProperties:
                  type: string
                description: Tags for the table
                type: object
            required:
            - hashKey
            - providerRef
            - tableName
            type: object
          status:
            description: DynamoDBTableStatus defines the observed state of DynamoDBTable
            properties:
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              itemCount:
                description: ItemCount is the approximate number of items
                format: int64
                type: integer
              lastSyncTime:
                description: LastSyncTime is the last time the table was synced
                format: date-time
                type: string
              ready:
                description: Ready indicates if the table is ready
                type: boolean
              streamARN:
                description: StreamARN is the ARN of the stream (if enabled)
                type: string
              tableARN:
                description: TableARN is the ARN of the table
                type: string
              tableSizeBytes:
                description: TableSizeBytes is the approximate size
                format: int64
                type: integer
              tableStatus:
                description: TableStatus is the current status
                type: string
            required:
            - ready
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
